<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirigir automáticamente todas las peticiones a la carpeta public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
